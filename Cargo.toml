[package]
name = "mnist"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "0.19.1", features = [
    "train",
    "vision",
    "metrics",
    "std",
    "fusion",
    "wgpu",
    "autotune",
], default-features = false }

rand = "0.9.2"
